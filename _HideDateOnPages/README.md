# _HideDateOnPages
React Wordpress Microsite plugin that adds a CSS snippet to the body of Terms and Privacy pages to hide the post date. Each of these pages already includes the date that they were updated within the body of the page.


## Installation
From the command line, run `react-wp --plugin --install _HideDateOnPages`.
Once installed, select `Y` when prompted to activate.
